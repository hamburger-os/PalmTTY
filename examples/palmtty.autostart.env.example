# Optional environment file for PalmTTY autostart.
# Copy this file outside the repository, replace the placeholder, and restrict
# it to the current OS user. On Linux use: chmod 600 <file>.
#
# Values are loaded literally; shell expansion is intentionally not performed.
PALMTTY_ACCESS_TOKEN=replace-with-a-long-random-secret
