{
  "name": "palmtty",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.11.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "preflight": "pnpm --filter @palmtty/protocol build && pnpm --filter @palmtty/config build && pnpm --filter @palmtty/agent run preflight",
    "dev": "pnpm run preflight && node scripts/dev.mjs",
    "start": "pnpm --filter @palmtty/agent start",
    "test": "pnpm -r --if-present test && pnpm scripts:test",
    "typecheck": "pnpm -r --if-present typecheck",
    "docs:check": "node scripts/docs-check.mjs",
    "theme:check": "node scripts/theme-check.mjs",
    "scripts:check": "node --check scripts/dev.mjs && node --check scripts/dev-network.mjs && node --check scripts/dev-network.test.mjs && node --check scripts/docs-check.mjs && node --check scripts/theme-check.mjs && node --check scripts/release-tools.mjs && node --check scripts/release-check.mjs && node --check scripts/release-notes.mjs && node --check scripts/license-check.mjs && node --check scripts/autostart-core.mjs && node --check scripts/autostart.mjs && node --check scripts/autostart.test.mjs && node --check scripts/release-tools.test.mjs",
    "scripts:test": "pnpm --filter @palmtty/config build && node --test scripts/dev-network.test.mjs scripts/release-tools.test.mjs scripts/autostart.test.mjs",
    "lint": "pnpm typecheck && pnpm scripts:check && pnpm docs:check && pnpm theme:check && pnpm version:check",
    "check": "pnpm lint && pnpm test && pnpm build",
    "version:check": "node scripts/release-check.mjs",
    "release:check": "node scripts/release-check.mjs --release",
    "release:notes": "node scripts/release-notes.mjs",
    "license:check": "node scripts/license-check.mjs",
    "autostart": "node scripts/autostart.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@10.17.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "node-pty"
    ]
  }
}
